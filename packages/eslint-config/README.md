# @fitlife/eslint-config

Shared ESLint configuration for FitLife monorepo.

## Usage

In your `eslint.config.js`:

```js
import baseConfig from '@fitlife/eslint-config';

export default [
  ...baseConfig,
  // Your overrides
];
```
